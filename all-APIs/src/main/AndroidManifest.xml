<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.POCsampleDev"
        tools:targetApi="31">
        <receiver
            android:name=".DevResponseReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.zebra.wirelessdeveloperservice.action.DEV_SERVICE_RESPONSE" />
                <action android:name="com.zebra.wirelessdeveloperservice.action.BT_PROXIMITY_STATE_CHANGE" />
                <action android:name="com.zebra.wirelessdeveloperservice.action.CONNECTION_STATE_CHANGE" />
                <!-- Add other actions or categories if needed -->
            </intent-filter>
        </receiver>

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>